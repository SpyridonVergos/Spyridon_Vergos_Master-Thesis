# Environment for running ATHEXNEURALNETS.ipynb
#
# The saved models (MODELS/MLP2.h5, MODELS/LSTM.h5) were created with
# Keras 2.0.9 / 2.1.5 and use the legacy list-style Sequential config.
# Keras 3 (bundled with TensorFlow >= 2.16) cannot load them
# (TypeError in the legacy h5 loader), so TensorFlow must be pinned to
# 2.15.x — the last release that ships Keras 2.
tensorflow-cpu==2.15.1
pandas
scikit-learn
matplotlib
jupyter
